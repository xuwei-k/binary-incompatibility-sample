val foldableOld = project.settings(
  organization := "com.example",
  name := "foldable",
  version := "0.1" 
)

val foldableNew = project.settings(
  organization := "com.example",
  name := "foldable",
  version := "0.2"
)
